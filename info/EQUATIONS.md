
$a+b=c$

## Absorption
$a_t = a_w + \Sigma a_p + \Sigma a_g$

$a(\lambda)_{uncorr} = \frac{1}{x} * ln(\frac{a(\lambda)_{signal}}{a(\lambda)_{reference}})$

$a(\lambda)_{m\_discontinuity} =  a(\lambda)_{offset}-a(\lambda)_{uncorr}-\Delta T_a$

## Attenuation
$c_t = c_w + \Sigma c_p + \Sigma c_g$

$c(\lambda)_{uncorr} = \frac{1}{x} * ln(\frac{c(\lambda)_{signal}}{c(\lambda)_{reference}})$
