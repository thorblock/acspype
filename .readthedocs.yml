version: 2

# Set the OS, Python version and other tools you might need
build:
  os: ubuntu-22.04
  tools:
    python: "3.9"

sphinx:
  configuration: docs/docs/source/conf.py

python:
  version: "3.9"
  install:
    - method: pip
      path: .
